Pine’s usual mathematical functions are not allowed on them. 
Special-purpose functions must be used to operate on all of an array’s values. 
The available functions are: 

array.avg(), array.min(), array.max(), array.median(), array.mode(), 
array.standardize(), array.stdev(), array.sum(), array.variance(), array.covariance(), array.range().
